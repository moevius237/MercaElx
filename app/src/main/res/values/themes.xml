<!-- res/values/themes.xml -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Mercaelx" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Tema normal -->
    </style>

    <!-- Tema para el splash screen -->
    <style name="Theme.Mercaelx.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#EFE5DA</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.Mercaelx</item>
    </style>
</resources>
